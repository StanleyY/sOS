var TSOS;
(function (TSOS) {
    var Scheduler = (function () {
        function Scheduler() {
        }
        return Scheduler;
    })();
    TSOS.Scheduler = Scheduler;
})(TSOS || (TSOS = {}));
